"""Benchmark harnesses for the Deterministic Horizon paper.

The ``quick`` subpackage is a stripped-down, low-cost validation suite that
recovers the per-model horizon ``d*`` in minutes instead of the full
multi-thousand-dollar sweep. See ``benchmarks/README.md``.
"""
